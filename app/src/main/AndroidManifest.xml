<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tk.divesdk.nutrifood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaContato"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".TelaCadastro" />
        <activity android:name=".TelaRecuperaSenha" />
        <activity android:name=".TelaTermos"></activity>
        <activity
            android:name=".TelaSobre"
            android:parentActivityName=".TelaPrincipal"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".TelaPrincipal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".TelaListaAlimentos"
            android:label="@string/app_name"
            android:parentActivityName=".TelaPrincipal"></activity>
        <activity
            android:name=".TelaSplash"
            android:configChanges="orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TelaAlimento"
            android:label="@string/app_name"
            android:launchMode="singleTop"></activity>
    </application>

</manifest>